'use strict';

module.exports = function(ngModule) {
  ngModule.controller('forgotPasswordPageController', function(
    $scope,
    $location,
    stateService
  ) {
    'ngInject';
    'use strict';
  });
};
