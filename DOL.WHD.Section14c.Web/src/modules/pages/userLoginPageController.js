'use strict';

module.exports = function(ngModule) {
  ngModule.controller('userLoginPageController', function(
    $scope,
    $location,
    stateService
  ) {
    'ngInject';
    'use strict';
  });
};
